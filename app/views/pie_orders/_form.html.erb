<%= form_for(@pie_order) do |f| %>
  <% if @pie_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pie_order.errors.count, "error") %> prohibited this pie_order from being saved:</h2>

      <ul>
      <% @pie_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pie_id %><br />
    <%= f.number_field :pie_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
